package com.ruchij.web

package object routes {
  object Paths {
    val `/user` = "/user"

    val `/session` = "/session"

    val `/health` = "/health"

    val `weight-entry` = "weight-entry"

    val user = "user"
  }
}
