package com.ruchij.web.routes

object Paths {
  val `/user` = "/user"

  val `/session` = "/session"

  val `/health` = "/health"

  val `/username` = "/username"

  val `weight-entry` = "weight-entry"

  val exists = "exists"
}
